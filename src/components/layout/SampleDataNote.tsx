/** Marks a section as populated with hard-coded sample data, not real user data. */
export function SampleDataNote() {
  return (
    <p className="mb-3 text-xs text-ink-faint">
      Sample data shown for layout preview — not connected to real data yet.
    </p>
  );
}
